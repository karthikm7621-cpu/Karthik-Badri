image: python:3.10-slim

stages:
  - lint
  - test

before_script:
  - pip install --upgrade pip
  - pip install -r requirements.txt

lint_code:
  stage: lint
  script:
    - flake8 app.py

test_app:
  stage: test
  script:
    - pytest app.py
